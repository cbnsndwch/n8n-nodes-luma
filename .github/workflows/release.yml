name: Release to npm

on:
  push:
    tags: 
      - 'v*.*.*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: ./package.json

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-
            
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          pnpm install || { echo "❌ Dependency installation failed"; exit 1; }
          echo "✅ Dependencies installed successfully"
        
      - name: Validate package with dry-run
        run: |
          echo "🔍 Validating package structure with dry-run..."
          npm pack --dry-run || { echo "❌ Package validation failed"; exit 1; }
          echo "✅ Package structure validated successfully"
        
      - name: Run comprehensive validation
        run: |
          echo "🔍 Running comprehensive pre-publish validation..."
          pnpm run prepublishOnly || { echo "❌ Pre-publish validation failed"; exit 1; }
          echo "✅ Pre-publish validation completed successfully"
        
      - name: Run tests
        run: |
          echo "🧪 Running test suite..."
          pnpm run test || { echo "❌ Test suite failed"; exit 1; }
          echo "✅ All tests passed successfully"
        
      - name: Validate package contents
        run: |
          echo "🔍 Validating final package contents..."
          # Check that dist/ directory exists and contains required files
          test -d dist || { echo "❌ Missing dist/ directory"; exit 1; }
          test -f dist/package.json || { echo "❌ Missing dist/package.json"; exit 1; }
          test -f dist/nodes/Luma/Luma.node.js || { echo "❌ Missing main node file"; exit 1; }
          test -f dist/credentials/LumaApi.credentials.js || { echo "❌ Missing credentials file"; exit 1; }
          
          # Verify package.json has correct name and version
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          echo "📋 Package: $PACKAGE_NAME@$PACKAGE_VERSION"
          echo "✅ Package contents validated successfully"
        
      - name: Validate npm authentication
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "🔐 Validating npmjs authentication..."
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "❌ NPM_TOKEN secret is not set. Please configure the NPM_TOKEN secret in repository settings."
            echo "💡 Go to: Repository Settings > Secrets and Variables > Actions > New repository secret"
            echo "💡 Name: NPM_TOKEN"
            echo "💡 Value: Your npm authentication token with publish permissions"
            exit 1
          fi
          
          # Test npmjs authentication by checking whoami
          CURRENT_USER=$(pnpm whoami) 
          if [ -z "$CURRENT_USER" ]; then
            echo "❌ npmjs authentication failed. The NPM_TOKEN may be invalid or expired."
            echo "💡 Please verify your npmjs token has the following permissions:"
            echo "   - Read and write access to packages"
            echo "   - Automation scope (if available)"
            echo "💡 Generate a new token at: https://www.npmjs.com/settings/tokens"
            echo "💡 Then update the NPM_TOKEN secret in repository settings"
            exit 1
          fi
                
          echo "✅ npmjs authentication successful as user: $CURRENT_USER"
        
      - name: Publish to npmjs
        run: |
          echo "🚀 Publishing package to npmjs..."
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          
          pnpm publish --access public || {
            echo "❌ npm publish failed for $PACKAGE_NAME@$PACKAGE_VERSION"
            echo "💡 Common causes and solutions:"
            echo "   - Version already exists: Check if this version is already published"
            echo "   - Authentication failed: Verify NPM_TOKEN secret is valid"
            echo "   - Permission denied: Ensure token has publish permissions for this package"
            echo "   - Network issues: Check npm registry connectivity"
            echo "💡 Manual verification commands:"
            echo "   npm view $PACKAGE_NAME versions --json  # Check existing versions"
            echo "   npm whoami                              # Verify authentication"
            echo "   npm access list packages                # Check package permissions"
            exit 1
          }
          echo "✅ Package $PACKAGE_NAME@$PACKAGE_VERSION published successfully to npm"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Verify publication
        run: |
          echo "🔍 Verifying package publication..."
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          
          # Wait a moment for npm registry to update
          sleep 10
          
          # Check if package is available on npm
          npm view "$PACKAGE_NAME@$PACKAGE_VERSION" version || { echo "⚠️ Package not yet available on npm registry (may take a few minutes)"; exit 0; }
          echo "✅ Package successfully published and available on npm registry"